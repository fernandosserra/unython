# .gitignore - Protocolo de Camuflagem Genial

# 1. ARTEFATOS DE AMBIENTE VIRTUAL (VENV)
# Essencial: Não envie o ambiente virtual!
/venv/
.venv/
/env/

# 2. ARTEFATOS DE SEGURANÇA
config/credentials.json
config/token.json
config/secrets.toml

# 3. ARTEFATOS DE DADOS E CACHE
# O banco de dados e os caches gerados pelo Python e pelo VS Code
/data/*.db             # O arquivo unython.db (O próprio banco de dados)
/data/*.sqlite
__pycache__/           # Cache de compilação de Python (não é código fonte)
**/__pycache__/
.vscode/               # Configurações locais do VSCode (se houver, evite conflitos)
.idea/                 # Configurações do PyCharm/IntelliJ (se houver)

# 4. Logs e Executáveis
*.log
*.exe